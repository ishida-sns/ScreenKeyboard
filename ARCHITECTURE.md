# アプリケーション構造

## プロジェクト概要

本プロジェクトは、Windows用のスクリーンキーボードとタッチパッドが一体化したC# WPFアプリケーションです。

## ファイル構成

```
ScreenKeyboard/
├── App.xaml                    # アプリケーションリソースとスタイル定義
├── App.xaml.cs                 # アプリケーションクラス
├── MainWindow.xaml             # メインウィンドウのUI定義
├── MainWindow.xaml.cs          # メインウィンドウのロジック
├── ScreenKeyboard.csproj       # プロジェクト設定
└── README.md                   # ドキュメント
```

## コード統計

- 総行数: 533行
- XAML: 259行 (UI定義)
- C#: 263行 (ロジック)
- プロジェクト設定: 11行

## アプリケーションレイアウト

```
┌─────────────────────────────────────────────────────────────────┐
│  スクリーンキーボード & タッチパッド                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────┐  ┌─────────────────────┐       │
│  │ キーボード                 │  │ タッチパッド         │       │
│  │                            │  │                     │       │
│  │ [1][2][3][4][5][6][7][8][9][0]│  │  ┌───────────────┐│       │
│  │ [Q][W][E][R][T][Y][U][I][O][P]│  │  │               ││       │
│  │ [A][S][D][F][G][H][J][K][L]   │  │  │  ドラッグして ││       │
│  │ [Z][X][C][V][B][N][M]         │  │  │  移動         ││       │
│  │ [Shift][  Space  ][Enter][⌫] │  │  │               ││       │
│  │ [Tab][Esc][Ctrl][Alt][Win]    │  │  └───────────────┘│       │
│  │ [!][@][#][$][%][&][*][(]      │  │                     │       │
│  │ [)][−][+][=][[][]][;][']      │  │  [左クリック][右クリック]  │
│  │ [,][.][/][\][`][~][<][>]      │  │                     │       │
│  └────────────────────────────┘  │  感度: [====○=====]   │       │
│                                    │       5x            │       │
│                                    └─────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

## 主要機能

### キーボード機能
- **文字入力**: アルファベット A-Z、数字 0-9
- **特殊文字**: 記号、句読点など
- **制御キー**: Space、Enter、Backspace、Tab、Esc、Ctrl、Alt、Win
- **Shiftキー**: トグル方式（オン/オフ）

### タッチパッド機能
- **カーソル移動**: ドラッグ操作でマウスカーソルを移動
- **クリック**: 左クリック・右クリックボタン
- **感度調整**: 1x～20xのスライダー調整

## Windows API使用

本アプリケーションは以下のWindows API（user32.dll）を使用しています：

- `keybd_event`: キーボード入力のシミュレーション
- `mouse_event`: マウスクリックのシミュレーション
- `SetCursorPos`: マウスカーソル位置の設定
- `GetCursorPos`: 現在のマウスカーソル位置の取得

## UI デザイン

- **テーマ**: ダークテーマ（Visual Studio風）
- **カラースキーム**: 
  - 背景: #1e1e1e
  - パネル: #252526
  - ボタン: #2d2d30
  - ボーダー: #3e3e42
  - アクセント: #007acc
- **レスポンシブ**: ウィンドウサイズに応じて調整可能

## ビルド要件

- .NET 6.0 SDK 以上
- Windows OS (実行環境)
- Visual Studio 2022 または VS Code (推奨)

## 使用制限

- Windows専用アプリケーション
- 一部のキーボード/マウス操作には管理者権限が必要な場合があります
- セキュリティソフトウェアによってブロックされる可能性があります
